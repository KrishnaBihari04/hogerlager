console.log('Main loaded');

function getRandomInt(max) {
    return Math.round(Math.random() * max);
  }
  const myNumber = getRandomInt(9);
  console.log(myNumber)


function ExampleFunction() {
    console.log("Hoi, dit is een voorbeeld");
}